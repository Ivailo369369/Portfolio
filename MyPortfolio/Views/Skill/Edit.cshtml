@model MyPortfolio.ViewModels.EditViewModel.EditSkillViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>@ViewData["Title"] <b>Skill</b></h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LevlPercent" class="control-label"></label>
                <input asp-for="LevlPercent" class="form-control" />
                <span asp-validation-for="LevlPercent" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
